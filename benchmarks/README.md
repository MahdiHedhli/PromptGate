# PromptGate Synthetic Benchmarks

This directory is reserved for public-safe benchmark harness modules. Generated benchmark outputs stay under ignored runtime/report paths unless they are summarized into docs.

Use synthetic data only.
